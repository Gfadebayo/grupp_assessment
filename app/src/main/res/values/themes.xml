<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.ProductExplorer.Splash" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:colorBackground">@color/ic_launcher_background</item>
        <!--        Will be no-op on v31 and above as the system splash screen is used instead-->
        <item name="android:windowBackground">@drawable/layer_splash</item>

        <item name="android:navigationBarColor">?android:attr/colorBackground</item>
        <item name="android:statusBarColor" >?android:attr/colorBackground</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.ProductExplorer" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:colorBackground">#FFF2F2F7</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brown</item>
        <item name="colorPrimaryVariant">@color/brown</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorSurface">?android:attr/colorBackground</item>
        <item name="colorOnSurface">#ff181c32</item>

        <item name="android:textViewStyle">@style/TextView.Default</item>
        <item name="android:textAppearance">@style/TextAppearance.Default</item>
        <item name="textAppearanceBody1">@style/TextAppearance.List.Title</item>
        <item name="textAppearanceBodyMedium">@style/TextAppearance.List.Price</item>
    </style>

    <style name="TextView.Default" parent="Widget.AppCompat.TextView">
        <item name="android:textAppearance">@style/TextAppearance.Default</item>
    </style>
</resources>